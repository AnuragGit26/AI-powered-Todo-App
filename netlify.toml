[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  # Add any build-time environment variables that are not sensitive
  NODE_VERSION = "22"